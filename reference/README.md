# ข้อสอบอ้างอิง

**ใช้ศึกษาแนวเท่านั้น ห้ามคัดลอกโจทย์ ตัวเลข ถ้อยคำ หรือรูปภาพ** (กติกาข้อ 3 ของโปรเจกต์)
ไฟล์ในโฟลเดอร์นี้เป็นลิขสิทธิ์ของผู้อื่น จึงถูกกันไว้ใน `.gitignore` ไม่ขึ้น GitHub
ย้ายเครื่องเมื่อไหร่ต้องเอา PDF มาวางเอง — ส่วนไฟล์ `DIGEST-*.md` ขึ้น GitHub ปกติเพราะเป็นงานเขียนของเราเอง

## วิธีอ่านไฟล์พวกนี้

**`pdftotext` อ่านภาษาไทยไม่ออก** ตัวอักษรไทยหายหมด เหลือแต่ตัวเลข และบางไฟล์เป็นสแกนล้วนไม่มี text layer เลย
เครื่องนี้ไม่มี `pdftoppm` ด้วย ทำให้ Read tool เปิด PDF ตรง ๆ ไม่ได้ → **ต้องเรนเดอร์เป็น PNG ด้วย PyMuPDF ก่อน**

```python
import pymupdf
d = pymupdf.open("reference/tgat2/TGAT2 Dek66.pdf")
d[7].get_pixmap(dpi=125).save("out.png")     # index เริ่มที่ 0 → หน้าที่ 8
```

text layer ใช้ได้อย่างเดียวคือหาว่าเลขข้อไหนอยู่หน้าไหน (ตัวเลขไม่หาย)

**อ่าน `DIGEST-*.md` ก่อนเสมอ** ถ้าแนวที่ต้องการถูกถอดไว้แล้ว จะได้ไม่ต้องเปิด PDF ซ้ำ

---

## tpat3/

| ไฟล์ | ชนิด | หมายเหตุ |
|---|---|---|
| `TPAT3 ธ.ค. 65.pdf` | **ข้อสอบจริง ธ.ค. 65** (ทำซ้ำโดยสำนักติว) | **น่าเชื่อถือที่สุด** · สแกนล้วน 48/48 หน้า · พาร์ทตัวเลขยาวถึงข้อ 16 |
| `TPATSCIENG_QUESTION_PAPER(1).pdf` | ชุดจำลอง — TCASter MOCK 17 ส.ค. 67 | ยืนยันจากหัวกระดาษแล้ว **ไม่ใช่ข้อสอบจริง ธ.ค. 67** · รูปแบบใกล้ของจริงที่สุด |
| `TPAT3 ชุด 3.pdf` | ชุดจำลอง — Exam Hub | พาร์ทตัวเลขนามธรรมล้วน 100% |
| `TPAT3 Examhub.pdf` | ชุดจำลอง — Exam Hub 4 ชุดในเล่มเดียว | ข้อ 1 ของแต่ละชุดอยู่หน้า 13 / 71 / 119 / 167 |
| `TPAT3.pdf` | **ยังไม่ระบุ** | 43 หน้า · ถึงข้อ 70 · หน้าปกอ้างถึงปี 2566 |
| `Tpat3(1).pdf` | **ยังไม่ระบุ** | 34 หน้า · ถึงข้อ 70 |
| `ตะลุย TPAT3 66.pdf` | **ยังไม่ระบุ** | 20 หน้า · เจอถึงข้อ 15 เท่านั้น · อ้างถึง TPAT 3 ปี 2566 |

- `DIGEST-part2.md` — ถอดแนวพาร์ทมิติสัมพันธ์จากของจริง 60 ข้อ (4 ไฟล์แรก)

**โครงข้อสอบ TPAT3** — 70 ข้อ 100 คะแนน · พาร์ท 1 (aptitude) 45 ข้อ 60 นาที แบ่ง 3 ท่อนละ 15:
ท่อน 1 ตัวเลข = *พาร์ทที่ 1* ของเรา · ท่อน 2 มิติสัมพันธ์ = *พาร์ทที่ 2* ของเรา · ท่อน 3 เชิงกล (ยังไม่ทำ)

---

## tgat2/

**เอาเฉพาะข้อ 81–140 ในไฟล์รวม / ข้อ 21–80 ในไฟล์เดี่ยว** — ติวเตอร์ไม่สอนท่อนความสามารถทางภาษา

### โครงข้อสอบ TGAT2 (ยืนยันจากคำชี้แจงที่พิมพ์ในข้อสอบ)

80 ข้อ · 100 คะแนน · 4 ท่อนละ 20 ข้อ 25 คะแนน · **แนวย่อย 16 แนว ท่อนละ 4 แนว แนวละ 5 ข้อพอดี**
หลักฐาน: `TGATV68_QUESTION_PAPER.pdf` หน้า 31 เป็นหน้าคำชี้แจงพิมพ์ผังครบ
และ `TGAT monkey 67.pdf` พิมพ์ชื่อแนวเดียวกันคำต่อคำเหนือแต่ละบล็อก — คนละสำนักพิมพ์ แปลว่านี่คือผังของ สทศ. ไม่ใช่สไตล์สำนักใดสำนักหนึ่ง

| ท่อน | แนวย่อย 4 แนว | ในไฟล์รวม |
|---|---|---|
| ความสามารถทางภาษา | สื่อความหมาย · ใช้ภาษา · อ่าน · เข้าใจภาษา | ข้อ 61–80 — **ข้าม** |
| ความสามารถทางจำนวน | อนุกรมมิติ · เปรียบเทียบเชิงปริมาณ · ความเพียงพอของข้อมูล · โจทย์ปัญหา | ข้อ 81–100 |
| ความสามารถด้านมิติสัมพันธ์ | แบบพับกล่อง · แบบหาภาพต่าง · แบบหมุนภาพสามมิติ · แบบประกอบภาพ | ข้อ 101–120 |
| ความสามารถทางเหตุผล | อนุกรมภาพ · อุปมาอุปไมยภาพ · สรุปความ · วิเคราะห์ข้อความ | ข้อ 121–140 |

| ไฟล์ | ชนิด | ข้อที่ใช้อยู่ที่ |
|---|---|---|
| `TGATV68_QUESTION_PAPER.pdf` | TGAT รวม 120 หน้า · มีหน้าคำชี้แจงผังข้อสอบ (น.31) | ข้อ 81–140 |
| `TGAT2 Dek66.pdf` | TGAT2 เดี่ยว 80 ข้อ · 31 หน้า | ข้อ 21–80 → หน้า 8–31 |
| `TGAT monkey 67.pdf` | TGAT รวม · MOCK EXAM TCAS67 ลงวันที่ 8 ต.ค. 2566 | ข้อ 81–140 |
| `TGAT Monkey 68.pdf` | TGAT รวม 84 หน้า | ข้อ 81–140 → หน้า 30–58 |
| `Examhub Tgat.pdf` | 272 หน้า · สำนักพิมพ์เดียวกับ TGATV68 (ไม่ใช่แหล่งอิสระ) | ชุดแรก ข้อ 21 หน้า 23 |

- `DIGEST.md` — ถอดแนวครบ 12 แนวย่อย พร้อมประเมินว่าใช้เครื่องผลิตเดิมซ้ำได้แค่ไหน

> **ทั้ง 5 ไฟล์ไม่มีไฟล์ไหนพิสูจน์ได้ว่าเป็นข้อสอบจริง** สี่ไฟล์ประกาศตัวเองว่าเป็นชุดจำลองหรือหนังสือฝึก
> และไม่มีเฉลยในไฟล์ไหนเลย — กฎของอนุกรมที่บันทึกไว้ใน DIGEST มาจากการอ่านด้วยตา ไม่ได้ยืนยันกับเฉลย
