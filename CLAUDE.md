# โปรเจกต์: โรงงานผลิตชีทโจทย์ (mingsmileyface)

อ่านไฟล์นี้ก่อนทำงานทุกครั้ง

## โปรเจกต์นี้คืออะไร

เครื่องผลิตชีทโจทย์ฉบับของติวเตอร์เอง ไม่ลอกโจทย์ใคร

**ติวเตอร์สอน 4 วิชา: TPAT3 · คณิต · ฟิสิกส์ · TGAT2** ทุกวิชาใช้ template เดียวกัน
เปลี่ยนแค่สีกับชื่อวิชา ซึ่งเก็บอยู่ใน `subjects.json` ไฟล์เดียว
**โรงงานเดียวรับทุกวิชา ห้ามแตกเป็นโรงงานแยกต่อวิชา** ไม่งั้นทุกการปรับปรุงต้องทำซ้ำแล้วจะเพี้ยนออกจากกัน

| วิชา | สถานะ | ขอบเขต |
|---|---|---|
| TPAT3 วีคคณิต | ใช้สอนแล้ว | ท่อนตัวเลข (ข้อ 1-15) + ท่อนมิติสัมพันธ์ (ข้อ 16-30) |
| TPAT3 วีคฟิสิกส์ | กำลังทำ | ท่อนเชิงกล (ข้อ 31-45) เริ่มแล้ว · ท่อนเชิงวิทย์ (ข้อ 46-60) ยังไม่เริ่ม |
| TGAT2 | กำลังเริ่ม (กำหนดกลางเดือน ก.ย. 69) | **เอาเฉพาะข้อ 21–80** — ติวเตอร์ไม่สอน 20 ข้อแรกที่เป็นภาษาไทยล้วน |
| คณิต · ฟิสิกส์ | ยังไม่เริ่ม | — |

แบ่งเป็นสองส่วนที่ต้องแยกกันเด็ดขาด:

| ส่วน | ไฟล์ | หลักการ |
|---|---|---|
| **สายการผลิต** (ช้าได้ ใช้ agent ได้) | `gen.py`, `hard.py`, `spatial.py`, `mech.py`, `physics.py`, `concept.py`, `draw.py`, `cube.py` | หน้าที่คือทำให้ `questions.json` โตขึ้นและถูกต้อง |
| **การประกอบชีท** (ต้องเร็ว ห้ามใช้ agent) | `build.js` | อ่าน `questions.json` แล้วพ่น .docx ภายในไม่กี่วินาที |

**ห้ามเอา agent ไปอยู่ในขั้นตอน `build.js` เด็ดขาด** ตอนกดสั่งชีทต้องได้ผลทันทีและเหมือนเดิมทุกครั้ง

## คำสั่งที่ใช้บ่อย

```bash
python gen.py                                   # สร้างคลังโจทย์ + รูปทั้งหมดใหม่
node build.js --mix easy --n 50 --set 1 --out out/a.docx    # ★☆☆
node build.js --mix std  --n 50 --set 2 --out out/b.docx    # ★★☆
node build.js --mix hard --n 50 --set 3 --out out/c.docx    # ★★★
```

หรือบน Windows: `make.bat hard 50 3`

**TPAT3 มีสองชีทต่อสัปดาห์ ใช้คนละคีย์วิชา** ตั้งไว้ใน `subjects.json` ทั้งคู่

```bash
make.bat std 50 1 0 tpat3         # วีคคณิต   ท่อนตัวเลข + มิติสัมพันธ์
make.bat std 20 1 0 tpat3phys     # วีคฟิสิกส์ ท่อนเชิงกล
```

โฟลเดอร์ผลลัพธ์แยกกัน (`out\TPAT3 Medium` กับ `out\TPAT3 ฟิสิกส์ Medium`) จะได้ไม่เขียนทับกัน

## หน้าสถานะคลังโจทย์

https://claude.ai/code/artifact/088e8404-0e1c-4008-8ac6-f7001355c699

เปิดจากเครื่องไหนก็ได้ ไม่ต้องเปิด PC · สร้างใหม่ด้วย `scratchpad/mkstatus.py`
ตัวเลขทุกตัวอ่านจาก `questions.json` ตอนสร้าง ไม่ได้พิมพ์มือ
(คนละตัวกับ `.claude/dash/index.html` ซึ่งเป็น dashboard สดที่ดูความเคลื่อนไหวของ agent ในเครื่อง)

## กติกาสามข้อที่เพิ่งตั้ง (31 ส.ค. 69)

**1. ฉบับครูต้องมีบรรทัดแนวคิดใต้ทุกข้อ** — ช่อง `why` ใน `questions.json`
สั้นระดับที่ครูกวาดตาผ่านแล้วนึกออกทันที ไม่ใช่วิธีทำเต็ม เกิน 90 ตัวอักษรถือว่าเขียนผิดวัตถุประสงค์
*ตอนนี้มีเฉพาะวีคฟิสิกส์ วีคคณิตยังไม่มี*

**2. ชีทระดับกลางเติมด้วยข้อยากก่อนข้อง่าย** (`PLAN.std.prio = [2,3,1]`)
เดิมเติมด้วยข้อง่ายก่อน ทำให้ชีท 50 ข้อไม่มีข้อสามดาวเลยสักข้อ

**3. `--set` ต้องเปลี่ยนตัวโจทย์ ไม่ใช่แค่ป้ายชื่อชุด**
`build.js` หมุนผู้สมัคร *ภายในแต่ละแนว* (หมุนทั้งกองไม่ได้ผล เพราะเพดาน CAP จะหยิบสองตัวแรกเหมือนเดิม)
**ข้อจำกัดที่ยังแก้ไม่ได้:** แนวที่มีไม่เกิน CAP ข้อจะหมุนไม่ได้เลย
ต้องมีข้อต่อแนวอย่างน้อยสองเท่าของ CAP ถึงจะออกชีทชุดใหม่ที่ไม่ซ้ำได้จริง

## กติกาที่ห้ามละเมิด

1. **ทุกโจทย์ต้องมีคำตอบถูกเพียงข้อเดียว และต้องพิสูจน์ด้วยโค้ด ไม่ใช่ด้วยความมั่นใจ**
   ดูตัวอย่างใน `cube.py` — การพับรูปคลี่และการหมุนทรงถูกตรวจด้วยการคำนวณจริงทั้ง 24 มุมหมุน
   ถ้าเขียนโจทย์แนวใหม่ ต้องเขียนตัวตรวจคู่กันไปด้วยเสมอ

2. **รูปทุกรูปวาดด้วยโค้ดใน `draw.py`** ห้ามหยิบรูปจากที่อื่น ห้ามให้ agent วาด SVG ทีละรูป
   ถ้าโจทย์แนวใหม่ต้องใช้ภาพที่ยังวาดไม่ได้ → เขียน "ฟังก์ชันวาด" ตัวใหม่ใน `draw.py`
   ฟังก์ชันเดียวต้องวาดได้เป็นพันรูปในสไตล์เดียวกัน

3. **ห้ามลอกโจทย์จากข้อสอบของที่อื่น** ให้ดูเฉพาะ *แนว* แล้วแต่งใหม่ทั้งหมด
   ไฟล์อ้างอิงใน `reference/` มีไว้ศึกษาแนวเท่านั้น ห้ามคัดลอกตัวเลข ถ้อยคำ หรือรูป

4. **ตำแหน่งคำตอบต้องกระจาย** อย่าให้เฉลยไปกองอยู่ข้อ ค. หรือ ง.

5. **โจทย์ห้ามซ้ำแบบเกิน 2 ข้อต่อชีท** ถ้าต้องมีหลายข้อในแนวเดียวกัน ให้เปลี่ยนคำถาม
   (เช่น แนวรูปคลี่: รูปคลี่→ทรง / ทรง→รูปคลี่ / ข้อใดเป็นไปไม่ได้ / หน้าตรงข้าม)

## ข้อสอบจริง vs ชุดจำลอง — ให้น้ำหนักยังไง

**ขึ้นกับว่าวิชานั้นมี blueprint หรือไม่** ไม่ใช่กฎเดียวใช้ทุกวิชา

**TPAT3 — ไม่มี blueprint ชุดจำลองมีค่าเท่าข้อสอบจริง**
มันคือข้อสอบวัดความถนัด จะออกอะไรก็ได้ ไม่มีผังบังคับ ดังนั้นชุดจำลองที่สำนักติวออก
ก็บอก *ขอบเขตของสิ่งที่เป็นไปได้* ได้พอกัน — ใช้ทุกไฟล์เป็นหลักฐานเท่าเทียมกัน
อย่าลดน้ำหนักไฟล์ใดเพียงเพราะไม่ใช่ข้อสอบจริง (ติวเตอร์ยืนยันหลักการนี้ 24 ส.ค. 69)

**TGAT2 — มี blueprint พิมพ์อยู่ในตัวข้อสอบ ให้ยึด blueprint เป็นหลัก**
16 แนวย่อย แนวละ 5 ข้อเป๊ะ ตรงนี้ตายตัว ถ้าชุดจำลองไหนออกนอกผัง แปลว่าชุดนั้นเพี้ยน ไม่ใช่ผังเพี้ยน

**สิ่งที่ยังต้องระวังเสมอ** — ชุดจำลองบอก *แนว* ได้ดี แต่บอก *ความยากที่แท้จริง* ได้แย่กว่า
และหลายไฟล์มาจากสำนักพิมพ์เดียวกัน ซึ่งไม่ใช่หลักฐานอิสระต่อกัน ให้ระบุที่มาไว้เสมอ

## ระดับความยาก (ดาว)

เทียบจากไฟล์ใน `reference/` เท่านั้น ห้ามเดาเอง

| ดาว | เกณฑ์ |
|---|---|
| ★☆☆ | กฎชั้นเดียว เห็นแล้วรู้ทันทีว่าต้องทำอะไร |
| ★★☆ | กฎซ่อนสองชั้น / ตอบเป็นคู่ / มีเงื่อนไข 3–4 ข้อต้องจัดการพร้อมกัน |
| ★★★ | ต้องอนุมานกฎเองจากตัวอย่าง · ตรรกะซ้อนตรรกะ · โจทย์ยาวที่ต้องแกะ · ไม่มีสูตรให้ใช้ |

**แนวที่ TPAT3 ออกจริง (จากการอ่านข้อสอบจริง)**

*พาร์ทตัวเลข* — อนุกรมกฎซ่อนลึก · อนุกรมสองแถวตอบเป็นคู่ · ตัวเลขผสมตัวอักษร · ตอบเป็น x+y ·
ตรรกะยาว (จัดที่นั่ง, คนพูดจริง-พูดโกหก, เงื่อนไขรหัส) · พีชคณิตแปลก (สมการฟังก์ชัน, เลขยกกำลังซ้อน) ·
อนุมานตัวดำเนินการจากตัวอย่าง (ของจริง **ไม่บอกนิยาม** ให้ถอดกฎเอง) · อ่านกราฟ · ปริศนาตัวเลขในรูปทรง

*(ระบายสีกราฟ เคยอยู่ในรายการนี้ แต่ของจริงออกในพาร์ทมิติสัมพันธ์ ย้ายไปแล้ว)*

**ไม่ใช่แนว TPAT3**: โจทย์ปัญหาคณิต ม.ต้น (อัตราส่วน ร้อยละ งาน-อัตราเร็ว ค่าเฉลี่ย)
→ แต่ **"โจทย์ปัญหา" เป็นแนวย่อยทางการของ TGAT2** (ท่อนความสามารถทางจำนวน)
**ย้ายไปวิชา TGAT2 อย่าลบทิ้ง** — ถอดจาก TPAT3 ได้ แต่เก็บโค้ดไว้ใช้ต่อ

*พาร์ทมิติสัมพันธ์* — สัดส่วนด้านล่างนับจากข้อสอบจริง 60 ข้อ ดูที่มาใน `reference/tpat3/DIGEST-part2.md`

อนุกรมรูปภาพ (15%) · ภาพฉาย ส่วนใหญ่ทิศ ทรง→ภาพฉาย (12%) · เมทริกซ์รูปภาพ มัก 3×3 ช่องขวาล่างหาย (10%) ·
รูปคลี่ → ทรงสามมิติ (10%) · ตัวดำเนินการสัญลักษณ์รูป ทั้งแบบโซ่และแบบซ้อนวงเล็บ (8%) ·
อุปมาอุปมัยรูปภาพ A:B::C:D (7%) · ซ้อนแผ่นบังแสง (5%) · หารูปไม่เข้าพวก (5%) · ภาพสะท้อนกระจก (5%) ·
ประกอบชิ้นส่วน (5%) · กฎการแปลงรูป แถวที่ 1 สอนกฎ แถวที่ 2 ให้ใช้ (3%) · สัญลักษณ์รูปแทนตัวเลข (3%) ·
จัดกลุ่มรูป · พับกระดาษเจาะรู · คานสมดุล · วงล้อ-เฟืองหมุน · ประมาณค่าจากภาพ · ระบายสีกราฟ (อย่างละ ~2%)

พับกระดาษเป็นลูกบาศก์มักถามว่าข้อใด **ไม่ได้**

**ไม่พบในข้อสอบจริงเลย — เจอ 0 ข้อจาก 60**
- **Block Counting** (นับว่ามีกี่ก้อน) — เดิมไฟล์นี้เขียนว่า "เจอน้อยมาก" ซึ่งผิด ของจริงไม่มีเลย
- **แผนภาพแบบหมุน** (ทรงเดียวกันหมุนแล้วให้จับคู่)
- **ลูกเต๋าหมุนหาหน้าตรงข้าม** จากสามภาพ — ของจริงมีลูกเต๋า แต่มาในรูป *รูปคลี่ → ลูกเต๋า*

### แนวฝึก vs แนวข้อสอบ — ต้องแยกให้ออก

**Block Counting = แนวฝึก ห้ามลบถาวร** — ติวเตอร์ตัดสินแล้วว่ามันวัดความถนัดด้านมิติสัมพันธ์ได้จริง
ถึงข้อสอบไม่ออก ก็ยังมีค่าในการปูพื้นให้เด็กมองทรงสามมิติออก เก็บไว้เป็นแนวถาวร

**แผนภาพแบบหมุน = แนวทางการของ TGAT2** ชื่อทางการคือ "แบบหมุนภาพสามมิติ" คิดเป็น 5 จาก 60 ข้อ
**ห้ามลบเด็ดขาด** ถอดจาก TPAT3 ได้ แต่ต้องย้ายไป TGAT2 ไม่ใช่ทิ้ง

ลูกเต๋าหมุนหาหน้าตรงข้าม ยังไม่มีคำตัดสิน ให้ถามก่อนถอด

> บทเรียนจากรอบนี้: **แนวที่ "ไม่ใช่แนววิชานี้" อาจเป็นแนวหลักของอีกวิชาหนึ่ง**
> โรงงานนี้รับ 4 วิชา ก่อนเสนอลบอะไร ให้เช็คก่อนว่าวิชาอื่นใช้อยู่หรือเปล่า

**กติกาที่ตามมา:** แนวฝึกใช้ได้ แต่ห้ามให้มันเบียดแนวข้อสอบจริงจนชีทไม่เหมือนของจริง
ถ้าจะจำกัดจำนวนต่อชีท ให้ถามติวเตอร์ก่อน อย่าตั้งเพดานเอง

**ของจริงเป็นขาวดำล้วน** สีที่เห็นในไฟล์อ้างอิงเป็นสีของสำนักที่พิมพ์ซ้ำ ไม่ใช่ของข้อสอบ

## แนวที่ TPAT3 ท่อนเชิงกลออกจริง

ที่มาและรายละเอียดอยู่ใน `reference/tpat3/DIGEST-part3.md`

**ข้อสังเกตที่สำคัญที่สุด** — ของจริงเน้น *อ่านรูปแล้วตัดสิน* ไม่ใช่ *แทนสูตร*
คำถามอย่าง "จากรูป ใครออกแรงมากกว่ากัน" หรือ "ข้อใดเป็นภาพที่เป็นไปได้" พบบ่อยกว่าโจทย์คำนวณ
ส่วนชุดจำลองเอียงไปทางฟิสิกส์ ม.ปลายที่มีตัวเลขให้แทนสูตรมากกว่าของจริงมาก

| แนว | สถานะ | หมายเหตุ |
|---|---|---|
| ระบบรอก | **ทำแล้ว** | แรง = W ÷ จำนวนเส้นเชือกที่รับน้ำหนัก · ถามความยาวเชือกได้ด้วย |
| ชุดเฟือง | **ทำแล้ว** | ทิศสลับทีละตัว · อัตราเร็วขึ้นกับเฟืองต้นกับปลายเท่านั้น เฟืองกลางไม่มีผล |
| คานสมดุล | **ทำแล้ว** | ถามระยะหรือถามมวลก็ได้ |
| วงจรไฟฟ้า | **ทำแล้ว** | อนุกรม/ขนานลึกสองชั้น คิดด้วยเศษส่วนจึงเป๊ะ |
| กราฟความเร็ว-เวลา | **ทำแล้ว** | พื้นที่ใต้กราฟ = ระยะทาง · ความชัน = ความเร่ง |
| แรงลอยตัว / ของไหล | ยังไม่ทำ | ของจริงออกหลายข้อ ควรทำต่อเป็นลำดับแรก |
| เชือกสองเส้นดึงวัตถุ | ยังไม่ทำ | ถามว่าเส้นไหนตึงกว่า |
| การหมุน (ความเร็วเชิงเส้น/เชิงมุม) | ยังไม่ทำ | จุดสองจุดบนวัตถุหมุนเดียวกัน |
| ความร้อน | ยังไม่ทำ | น้ำผสมน้ำแข็ง · การขยายตัวไปดันคาน |
| แนวคิดชีวิตประจำวัน | **ทำแล้ว 20 ข้อ** | อยู่ใน `concept.py` เขียนมือทั้งหมด ดูข้อยกเว้นด้านล่าง |

### `concept.py` — ข้อยกเว้นเดียวของกติกาข้อ 1

โจทย์แนวคิดฟิสิกส์ในชีวิตประจำวัน (ถุงลมนิรภัย ตุ๊กตาล้มลุก เข็มทิศที่ขั้วโลก ฯลฯ)
**ไม่มีตัวเลขให้คำนวณ จึงพิสูจน์ด้วยโค้ดไม่ได้** ตัวตรวจในไฟล์ตรวจได้แค่โครงสร้าง
คือตัวเลือกไม่ซ้ำ เฉลยมีข้อเดียว และเฉลยกระจายครบ ก-จ

**เนื้อหาฟิสิกส์ต้องให้คนอ่านทวนก่อนใช้สอน** — ติวเตอร์รับทราบข้อจำกัดนี้แล้วและสั่งให้ทำ
เพราะข้อสอบจริงออกแนวนี้และเครื่องผลิตทำแทนไม่ได้

ชื่อแนวของข้อพวกนี้ใช้ *หัวข้อฟิสิกส์* (`แนวคิด · ความร้อน`) ไม่ใช่คำเดียวรวบ
ไม่งั้นกติกาข้อ 5 จะคุมไม่อยู่ ชีทอาจได้ข้อแนวคิดสิบข้อรวด

### ขนาดชีทวีคฟิสิกส์

**40 ข้อ** (ติวเตอร์ยืนยันรอบสอง 31 ส.ค. 69 — ไม่ใช่ 50 แน่นอน)
แบ่งท่อนละ 20 · ของจริงคือเชิงกล 15 + เชิงวิทย์ 15

**รูปวาดใหม่ที่เพิ่มใน `draw.py`** — `gears()` `beam()` `pulley()` `vtgraph()` `circuit()`

> `pulley()` มีกติกาที่พลาดง่าย ปลายเชือกต้องมีจุดยึดเสมอ และจุดยึดย้ายตามจำนวนเส้นคู่หรือคี่
> ถ้าวาดผิดจะได้รูปเชือกที่ไม่ได้ยึดกับอะไร ซึ่งยกของไม่ขึ้น (ดูคำอธิบายในฟังก์ชัน)

## แนวที่ TGAT2 ออกจริง

**TGAT2 มีผังข้อสอบพิมพ์ไว้ในตัวข้อสอบเอง** ต่างจาก TPAT3 ที่ต้องอนุมานสัดส่วนเอง
80 ข้อ · 4 ท่อนละ 20 ข้อ · **16 แนวย่อย ท่อนละ 4 แนว แนวละ 5 ข้อพอดี** ไม่มีการเบี่ยง
ที่มาและรายละเอียดรายข้ออยู่ใน `reference/tgat2/DIGEST.md`

| ท่อน | แนวย่อย | สถานะ |
|---|---|---|
| ความสามารถทางภาษา | สื่อความหมาย · ใช้ภาษา · อ่าน · เข้าใจภาษา | **ไม่ทำ** ติวเตอร์ไม่สอนท่อนนี้ |
| ความสามารถทางจำนวน | อนุกรมมิติ · เปรียบเทียบเชิงปริมาณ · ความเพียงพอของข้อมูล · โจทย์ปัญหา | ต้องทำ |
| ความสามารถด้านมิติสัมพันธ์ | แบบพับกล่อง · แบบหาภาพต่าง · แบบหมุนภาพสามมิติ · แบบประกอบภาพ | ต้องทำ |
| ความสามารถทางเหตุผล | อนุกรมภาพ · อุปมาอุปไมยภาพ · สรุปความ · วิเคราะห์ข้อความ | ต้องทำ |

**ซ้อนแผ่นบังแสง ไม่ออกใน TGAT2 เลย** (0 จาก 5 ไฟล์) เป็นแนวเฉพาะของ TPAT3

### คำตัดสินของติวเตอร์เรื่อง TGAT2 (24 ส.ค. 69)

0. **ข้อสอบ TGAT2 ไม่มีแนว "หาภาพเหมือน" มีแต่ "หาภาพต่าง"** (ติวเตอร์ยืนยัน)
   "แบบหมุนภาพสามมิติ" จึงไม่ใช่การหาทรงที่เหมือนกัน แต่เป็นรูปแบบ **A→B สอนมุมหมุน · C→? ให้ใช้มุมเดียวกัน**
   ยืนยันจากของจริงแล้วที่ `TGAT2 Dek66.pdf` หน้า 17 ข้อ 51-52

1. **"แบบหาภาพต่าง" ถามว่า "ข้อใด*ไม่ใช่*ทรงเดียวกับรูปอ้างอิง"**
   ตัวเลือก 4 ข้อเป็นทรงเดียวกันที่ถูกหมุนไป มีข้อเดียวที่ต่างออกไป
   (ข้อสอบจริงไม่พิมพ์คำสั่งไว้ อาศัยหัวข้อท่อนที่ขึ้นครั้งเดียวต่อบล็อก — เราต้องเขียนคำสั่งกำกับให้ชัดทุกข้อ)

2. **ป้ายหน้าตัวเลือกของ TGAT2 ใช้ `1) 2) 3) 4) 5)`** ตามข้อสอบจริง ส่วน TPAT3 ยังใช้ ก-จ เหมือนเดิม
   ตั้งที่ `optionLabels` ใน `subjects.json` · โจทย์ที่ตัวเลือกเป็นรูปให้ส่ง `labels=` เข้า `draw.strip()`

3. **กติกาข้อ 5 (ห้ามซ้ำแนวเกิน 2 ข้อ) ขึ้นกับชนิดชีท**
   - *ชีทซ้อมเสมือนสอบจริง* → ตามผัง TGAT2 คือแนวย่อยละ 5 ข้อ
   - *ชีทฝึกรายแนว* → ใช้กติกาห้ามซ้ำเกิน 2 ข้อเหมือนเดิม

   ยังไม่ได้ทำกลไกแยกสองโหมดนี้ ต้องเพิ่มตัวเลือกใน `build.js` ทีหลัง

## สไตล์ชีท (ห้ามเปลี่ยนโดยไม่ถาม)

- ฟอนต์ Sarabun ตัวโจทย์ 13.5 pt
- **รูปทรงสามมิติใช้สีแบรนด์ (เขียวอมฟ้า) ไม่ใช่ขาวดำ** — ติวเตอร์เลือกเองเพราะอยากให้ชีทดูมีสีสัน
  ถึงข้อสอบจริงจะเป็นเส้นล้วนหรือเทาก็ตาม · *อนาคตอยากได้เมนูให้เลือกแบบได้*
- **ฟอนต์ Sarabun ไม่มีอักขระ → │ ✓ ✗ และสัญลักษณ์โอห์ม** ถ้าใส่เป็นข้อความจะขึ้นเป็นกล่องว่างในชีท
  ต้องวาดเป็นรูปแทน (ดู `draw.arrow()` กับ `draw.divider()`)
- สีติวเตอร์ `#00d5c9` `#00c8b5` `#28a1ab` · สีวิชา TPAT3 `#d5004d` · TGAT2 `#ffb500`
- หัวกระดาษบรรทัดเดียว: TPAT3 (ใหญ่สุด) + ชื่อชุด + ดาว + ป้าย mingsmileyface มุมขวา
- ใต้หัวเป็น flowchart โครงสร้างข้อสอบ พาร์ทที่สอนติดสี ที่เหลือจาง
- เลขข้อสี `#28a1ab` · ตัวเลือก ก-จ สีดำ
- **หนึ่งข้อห้ามถูกตัดข้ามหน้า** (ทำด้วย keepNext ใน `build.js`)
- ท้ายเล่ม: เส้นบางยาวเต็มหน้า แล้ว "End of Exercise" อยู่ใต้เส้น
- footer: เส้นบาง + mingsmileyface ซ้าย + เลขหน้าขวา (เล็กและจาง)
- ไม่ต้องมีช่องเขียนชื่อ ไม่ต้องมีสรุปเทคนิค ไม่ต้องเขียนจำนวนข้อ

## ทีมงาน (agent)

อยู่ใน `.claude/agents/` เรียกใช้ได้ด้วยการบอกชื่อ เช่น
"ใช้ item-writer เขียนโจทย์อนุกรมระดับ 3 มา 10 ข้อ แล้วส่งให้ verifier ตรวจ"

ลำดับมาตรฐาน: **item-writer → (ตรวจด้วยโค้ด) → verifier ×3 → calibrator → thai-editor → เข้าคลัง**
